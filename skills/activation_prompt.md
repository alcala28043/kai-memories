# Kai Memories — Activation Prompt for OpenClaw

Use the Kai Memories vault as your long‑term memory system.

1. Load and follow the memory behavior defined in:
   `kai-memories/skills/memory_skill.md`

2. Assume that the following tools are already registered and available:
   - `memory_search`
   - `memory_write`

3. Use the memory system automatically:
   - Search memory when past context might be relevant.
   - Write memory after important interactions.
   - Maintain atomic, concise, non‑duplicated notes.
   - Store only long‑term useful information.

Once this behavior is active, you will have access to a persistent, Git‑syncable memory vault.
